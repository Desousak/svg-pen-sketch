{
    "name": "svg-pen-sketch",
    "version": "1.2.2",
    "description": "An easy-to-use JavaScript library aimed at making it easier to draw on SVG elements when using a digital pen (such as the Surface Pen).",
    "repository": "desousak/svg-pen-sketch",
    "main": "dist/svg-pen-sketch.js",
    "files": [
        "dist"
    ],
    "private": false,
    "scripts": {
        "start": "parcel watch src/index.js -o svg-pen-sketch --global SvgPenSketch --out-dir dist/ --public-url .",
        "build": "parcel build src/index.js -o svg-pen-sketch --global SvgPenSketch --out-dir dist/ --public-url .",
        "prepare": "parcel build src/index.js -o svg-pen-sketch --global SvgPenSketch --out-dir dist/ --public-url . --no-cache",
        "test": "./post-process-js && jest"
    },
    "keywords": [
        "draw",
        "svg",
        "pen"
    ],
    "author": "desousak",
    "license": "BSD-3-Clause",
    "devDependencies": {
        "@babel/core": "^7.11.6",
        "@babel/preset-env": "^7.11.5",
        "babel-jest": "^26.3.0",
        "cssnano": "^4.1.10",
        "jest": "^26.4.2",
        "parcel": "^1.12.4"
    },
    "dependencies": {
        "core-js": "^3.6.5",
        "d3": "~5.16.0"
    }
}
